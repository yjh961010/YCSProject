<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.neoheulge.notice.service.NoticeService">
<insert id="insertNotice" parameterType="NoticeDTO">
    insert into nenotice values(nenotice_seq.nextval, #{author}, #{content}, SYSDATE ,#{views},#{RE_STEP},#{RE_LEVEL},#{TITLE});
</insert>
</mapper>